<%= form_with(model: user_ministry, local: true) do |form| %>
  <% if user_ministry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_ministry.errors.count, "error") %> prohibited this user_ministry from being saved:</h2>

      <ul>
      <% user_ministry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_ministry_user_id %>
  </div>

  <div class="field">
    <%= form.label :ministry_id %>
    <%= form.text_field :ministry_id, id: :user_ministry_ministry_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
