<%= form_for(current_user.user_ministries.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :ministry_id, @ministry.id %></div>
  <%= f.submit "Follow", class: 'btn btn-rose btn-round' %>
<% end %>
